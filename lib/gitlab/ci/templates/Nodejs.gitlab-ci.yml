# Official framework image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/node/tags/
image: node:latest

# Pick zero or more services to be used on all builds.
# Only needed when using a docker container to run your tests in.
# Check out: http://docs.gitlab.com/ce/ci/docker/using_docker_images.html#what-is-a-service
services:
  - mysql:latest
  - redis:latest
  - postgres:latest

# This folder is cached between builds
# http://docs.gitlab.com/ce/ci/yaml/README.html#cache
cache:
  paths:
    - node_modules/

#Make sure the project's root directory contains the .npmrc with the path to the @scope:registry and the path to where the packages are to be published/installed
#Uncomment the line starting with "build_package" if you don't have an .npmrc for your project

###
## TO TEST THE PACKAGE BUILD STEPS, UNCOMMENT build_package AND COMMENT OUT INSTALL PACKAGE
###
# build_package:
#   script:
#     # - echo "@scope/packge_name=https://gitlab.example.com/api/v4/packages/npm" >> .npmrc
#     # - echo "//gitlab.example.com/api/v4/projects/<project_id>/packages/npm/:_authToken=<oauth_token>" >> .npmrc
#     - cat .npmrc #to review the contents of your .npmrc
#     - npm publish

install_package:
  script:
    - npm install @scope/package_name
    - node node_modules/@scope/package_name/index.js
